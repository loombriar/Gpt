(()=>{const layer=document.querySelector('.fireflies');if(layer){const count=window.innerWidth<650?34:64;for(let i=0;i<count;i++){const f=document.createElement('i');f.className='firefly';f.style.left=Math.random()*100+'%';f.style.top=Math.random()*100+'%';f.style.setProperty('--dur',(8+Math.random()*18)+'s');f.style.setProperty('--blink',(1.4+Math.random()*3.8)+'s');f.style.setProperty('--delay',(-Math.random()*14)+'s');const rx=()=>Math.round((Math.random()-.5)*220)+'px';const ry=()=>Math.round((Math.random()-.5)*180)+'px';f.style.setProperty('--x1',rx());f.style.setProperty('--y1',ry());f.style.setProperty('--x2',rx());f.style.setProperty('--y2',ry());f.style.setProperty('--x3',rx());f.style.setProperty('--y3',ry());layer.appendChild(f)}}
const links=[...document.querySelectorAll('a[href^="#"]')];links.forEach(a=>a.addEventListener('click',e=>{const id=a.getAttribute('href');const t=id&&document.querySelector(id);if(t){e.preventDefault();t.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'center'})}}));
const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.animate([{opacity:.45,transform:'translateY(28px)'},{opacity:1,transform:'translateY(0)'}],{duration:700,easing:'cubic-bezier(.2,.7,.2,1)',fill:'both'})}),{threshold:.15});document.querySelectorAll('.deck').forEach(x=>io.observe(x));
})();